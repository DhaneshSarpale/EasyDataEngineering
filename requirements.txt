# ------------------------------------------------------------------
# DataForge - runtime dependencies
# Install with: pip install -r requirements.txt
# ------------------------------------------------------------------

# Core data manipulation
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0

# Synthetic data generation
Faker==25.2.0

# Configuration
PyYAML==6.0.1

# REST API mock + ingestion
flask==3.0.3
requests==2.32.3

# SFTP client (used by SFTP ingestion pattern)
paramiko==3.4.0

# Distributed transforms (heavy; optional for pure-pandas local runs)
pyspark==3.5.1

# Data quality
great-expectations==0.18.16

# XML / Avro file formats
lxml==5.2.2
fastavro==1.9.4

# AWS SDK (AWS-mode ingestion / streaming; local mode needs no credentials)
boto3==1.34.113

# CLI ergonomics
click==8.1.7
tabulate==0.9.0
