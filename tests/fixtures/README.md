# Test fixtures

Static fixture files for tests live here. Most tests generate their own
tiny, deterministic dataset in a temp directory via the `isolated_env`
fixture in `tests/conftest.py`, so few static fixtures are needed.
